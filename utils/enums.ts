

// upload status
export enum UploadStatus{
    ONSTART,UPLOADED
  }

// volume enumState
export enum VolumeState{
    NORMAL,MUTED,UNMUTED,LOAD
}

export enum DownloadStatus{
    ONPAUSE,ONSTART,ONDOWNLOADING,DOWNLAODED
}
